127.0.0.1       localhost localhost.localdomain localhost4 localhost4.localdomain4
{% for host in groups['replset_server'] %}
{{ hostvars[host]['ansible_' + iface]['ipv4']['address'] }}	{{ host }}
{% endfor %}
